// Copy this file to `ws-config.js` and update the WS_URL to your server's public address.
// Example:
// window.WS_URL = 'wss://your-server.example.com';

// For local development with the bundled server.js running on port 8000:
// window.WS_URL = 'ws://localhost:8000';

// For secure (wss) use the wss:// protocol and a valid public domain.
// window.WS_URL = 'wss://example.com';
